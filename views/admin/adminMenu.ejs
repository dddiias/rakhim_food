<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Административное меню</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="/">RakhimFood Admin</a>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/admin/logout">Выйти</a>
                </li>
            </ul>
        </nav>
    </header>
    <div class="container">
        <h1 class="mt-5">Управление меню</h1>
        <a href="/admin/menu/new" class="btn btn-primary mt-3 mb-3">Добавить новый продукт</a>
        <ul class="list-group">
            <% products.forEach(product=> { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <%= product.name %> - <%= product.price %>
                            <div>
                                <a href="/admin/products/edit/<%= product._id %>"
                                    class="btn btn-primary mr-2">Редактировать</a>
                                <form action="/admin/menu/delete/<%= product._id %>" method="POST" class="d-inline">
                                    <button type="submit" class="btn btn-danger">Удалить</button>
                                </form>
                            </div>
                </li>
                <% }); %>
        </ul>
    </div>
</body>

</html>